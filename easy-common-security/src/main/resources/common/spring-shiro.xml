<?xml version="1.0" encoding="UTF-8" ?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
	xmlns:tx="http://www.springframework.org/schema/tx"
	xmlns:context="http://www.springframework.org/schema/context" 
	xmlns:p="http://www.springframework.org/schema/p"
	xmlns:aop="http://www.springframework.org/schema/aop"
	xsi:schemaLocation="
    http://www.springframework.org/schema/beans 
    http://www.springframework.org/schema/beans/spring-beans-4.2.xsd
    http://www.springframework.org/schema/context 
    http://www.springframework.org/schema/context/spring-context-4.2.xsd
    http://www.springframework.org/schema/tx 
    http://www.springframework.org/schema/tx/spring-tx-4.2.xsd
    http://www.springframework.org/schema/aop 
    http://www.springframework.org/schema/aop/spring-aop-4.2.xsd
    ">
    
    <!-- =========================================================
         Shiro Core Components - Not Spring Specific
         ========================================================= -->
    <!-- Shiro's main business-tier object for web-enabled applications
    (use DefaultSecurityManager instead when there is no web environment)-->
    <bean id="securityManager" class="org.apache.shiro.web.mgt.DefaultWebSecurityManager">
<!--         <property name="cacheManager" ref="cacheManager"/> -->
        <property name="sessionManager" ref="sessionManager"/>
        <property name="rememberMeManager" ref="rememberMeManager"/>
        <!-- Single realm app.  If you have multiple realms, use the 'realms' property instead. -->
        <property name="realm" ref="shiroSecurityRealm"/>
    </bean>
    <!-- Let's use some enterprise caching support for better performance.  You can replace this with any enterprise
         caching framework implementation that you like (Terracotta+Ehcache, Coherence, GigaSpaces, etc -->
    <!-- ::缓存管理器 jedis实现 -->
	<bean id="cacheManager" class="com.yffd.easy.demo.shiro.custom.cache.JedisCacheManager">
		<property name="jedisManager" ref="jedisManager"/>
	</bean>

    <!-- Used by the SecurityManager to access security data (users, roles, etc).
         Many other realm implementations can be used too (PropertiesRealm,
         LdapRealm, etc. -->
    <!-- ::安全数据源 -->
    <bean id="securityRealm" class="com.yffd.easy.demo.shiro.custom.realm.SecurityRealm">
    	<property name="securityService" ref="securityService"/>
    	<!-- 指定凭证匹配器 -->
        <property name="credentialsMatcher" ref="credentialsMatcher"/>
    </bean>
    <!-- ::凭证匹配器 -->
    <bean id="credentialsMatcher" class="com.yffd.easy.demo.shiro.custom.credential.RetryLimitHashedCredentialsMatcher"/>
	<!-- ::认证策略 -->
	<bean id="authenticator" class="org.apache.shiro.authc.pam.ModularRealmAuthenticator">
    	<property name="authenticationStrategy">
    		<bean class="org.apache.shiro.authc.pam.AtLeastOneSuccessfulStrategy"/>
    	</property>
    </bean>
    
    
    <!-- =========================================================
         Shiro Spring-specific integration
         ========================================================= -->
    <!-- Post processor that automatically invokes init() and destroy() methods
         for Spring-configured Shiro objects so you don't have to
         1) specify an init-method and destroy-method attributes for every bean
            definition and
         2) even know which Shiro objects require these methods to be
            called. -->
    <bean id="lifecycleBeanPostProcessor" class="org.apache.shiro.spring.LifecycleBeanPostProcessor"/>

    <!-- Enable Shiro Annotations for Spring-configured beans.  Only run after
         the lifecycleBeanProcessor has run: -->
    <bean class="org.springframework.aop.framework.autoproxy.DefaultAdvisorAutoProxyCreator"
          depends-on="lifecycleBeanPostProcessor"/>
    <bean class="org.apache.shiro.spring.security.interceptor.AuthorizationAttributeSourceAdvisor">
        <property name="securityManager" ref="securityManager"/>
    </bean>

    <!-- Define the Shiro Filter here (as a FactoryBean) instead of directly in web.xml -
         web.xml uses the DelegatingFilterProxy to access this bean.  This allows us
         to wire things with more control as well utilize nice Spring things such as
         PropertiesPlaceholderConfigurer and abstract beans or anything else we might need: -->
    <!-- 对应于web.xml中配置的那个shiroFilter名称 -->
	<!-- 1、先执行Shiro自己的Filter链；2、再执行Servlet容器的Filter链（即原始的Filter）-->
    <bean id="shiroFilter" class="org.apache.shiro.spring.web.ShiroFilterFactoryBean">
        <!-- Shiro的核心安全接口，这个属性是必须的 -->
        <property name="securityManager" ref="securityManager"/>
        <!-- 要求登录时的链接(登录页面地址)，非必须的属性，默认会自动寻找Web工程根目录下的"/login.jsp"页面 -->
        <property name="loginUrl" ref="loginUrl"/>
        <!-- 登录成功后要跳转的连接(本例中此属性用不到，因为登录成功后的处理逻辑在LoginController里硬编码) -->
        <property name="successUrl" ref="successUrl"/>
        <!-- 用户访问未对其授权的资源时，所显示的连接 -->
        <property name="unauthorizedUrl" ref="unauthorizedUrl"/>
        <!-- The 'filters' property is not necessary since any declared javax.servlet.Filter bean
             defined will be automatically acquired and available via its beanName in chain
             definitions, but you can perform overrides or parent/child consolidated configuration
             here if you like: -->
       <property name="filters" ref="filters"/>
       <property name="filterChainDefinitionMap" ref="filterChainDefinitionMap"/>
        <!-- 
        <property name="filters">
           <util:map>
              <entry key="login" value-ref="loginPathMatchingFilter"/>
              <entry key="role" value-ref="rolePathMatchingFilter"/>
              <entry key="permission" value-ref="permissionPathMatchingFilter"/>
           </util:map>
       </property>
        <property name="filterChainDefinitions">
            <value>
                /favicon.ico = anon
                /logo.png = anon
                /shiro.css = anon
                /s/login = anon
                # allow WebStart to pull the jars for the swing app:
                /*.jar = anon
                # everything else requires authentication:
                /** = authc
            </value>
        </property>
        -->
    </bean>
	<!-- shiroFilter configuration factory -->
    <bean id="shiroConfigFactory" class="com.yffd.easy.common.security.shiro.config.ShiroConfigFactory"/>
    <!-- shiroFilter configuration properties -->
    <bean id="loginUrl" factory-bean="shiroConfigFactory" factory-method="createLoginUrl"/>
    <bean id="successUrl" factory-bean="shiroConfigFactory" factory-method="createSuccessUrl"/>
    <bean id="unauthorizedUrl" factory-bean="shiroConfigFactory" factory-method="createUnauthorizedUrl"/>
    <bean id="filters" factory-bean="shiroConfigFactory" factory-method="createFilters"/>
    <bean id="filterChainDefinitionMap" factory-bean="shiroConfigFactory" factory-method="createFilterChainDefinitionMap"/>
    
    <!-- ::自定义shiro拦截器 -->
	<bean id="loginPathMatchingFilter" class="com.yffd.easy.demo.shiro.custom.filter.LoginPathMatchingFilter"/>
	<bean id="rolePathMatchingFilter" class="com.yffd.easy.demo.shiro.custom.filter.RolePathMatchingFilter"/>
	<bean id="permissionPathMatchingFilter" class="com.yffd.easy.demo.shiro.custom.filter.PermissionPathMatchingFilter"/>
	
    <!-- =========================================================
         Shiro Cache-Jedis integration
         ========================================================= -->
         
</beans>